require 'spec_helper'

describe WebinarjamApi do
  it 'has a version number' do
    expect(WebinarjamApi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
